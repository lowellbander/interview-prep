public HashMap<Integer, Student> buildMap (Student[] students) {
    HashMap<Integer, Student> mapping = new HashMap<Integer, Student>;
    for (Student s : students) map.puy(s.getID(), s);
    return mapping;
}
